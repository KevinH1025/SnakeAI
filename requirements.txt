# Install torch first from the CPU index for a much smaller download:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
torch>=2.2
numpy>=1.26
pygame>=2.5
matplotlib>=3.8
pytest>=8.0

# optional. If installed, the region labelling in the environment runs compiled,
# which is roughly 20x faster. Everything works without it, just slower.
numba>=0.59
